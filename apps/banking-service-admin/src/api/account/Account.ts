export type Account = {
  accBalance: number | null;
  accNumber: string | null;
  accountNumber: string | null;
  accountType?: "Option1" | null;
  balance: number | null;
  createdAt: Date;
  id: string;
  updatedAt: Date;
};
